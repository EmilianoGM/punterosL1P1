#include <stdio.h>
#include <stdlib.h>

int main()
{
    FILE* pArchivo;
    pArchivo = fopen("Data.csv, "r");
    while(!feof(pFile))
    {
        fscanf(pArchivo,)
    }
    fclose(pArchivo);

    return 0;
}
